# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: "CI workflow preview [PR preview]"
'on': pull_request
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install dependencies
        run: bash cli-dev.sh ci && yarn install --immutable

      - name: Building rinkeby [preview]
        run: yarn run ci:build:rinkeby

      - name: Deploying rinkeby [preview]
        uses: matter-labs/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZSYNC_DAPP_CHECKOUT }}'
          target: prod-rinkeby
          projectId: zsync-dapp-checkout
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
          
      - name: Building rosten [preview]
        run: yarn run ci:build:ropsten

      - name: Deploying ropsten [preview]
        uses: matter-labs/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZSYNC_DAPP_CHECKOUT }}'
          target: prod-ropsten
          projectId: zsync-dapp-checkout
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels

      - name: Building mainnet [preview]
        run: yarn run ci:build:prod

      - name: Deploying mainnet [preview]
        uses: matter-labs/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZSYNC_DAPP_CHECKOUT }}'
          target: prod-mainnet
          projectId: zsync-dapp-checkout
        env:
          FIREBASE_CLI_PREVIEWS: hostingchannels
