# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: "Deploy testnets [rinkeby, ropsten]"
"on":
  push:
    branches:
      - develop
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - uses: actions/checkout@v2
      - run: "bash cli-dev.sh ci && yarn run ci:build:rinkeby"
      - uses: matter-labs/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZSYNC_DAPP_CHECKOUT_PROD }}"
          target: prod-rinkeby
          projectId: zsync-dapp-checkout
          channel: live

      - name: "Build ropsten"
        run: "yarn run ci:build:ropsten"

      - name: "Deploy static to the ropsten"
        uses: matter-labs/action-hosting-deploy@v0
        with:
          repoToken: "${{ secrets.GITHUB_TOKEN }}"
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZSYNC_DAPP_CHECKOUT_PROD }}"
          target: prod-ropsten
          projectId: zsync-dapp-checkout
          channel: live