# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: "Deploy production"
"on":
  push:
    branches:
      - master
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    environment: prod
    steps:
      - uses: actions/checkout@v2
      - run: bash cli-dev.sh ci

      - name: "Build mainnet [prod]"
        run: yarn run ci:build:prod
      - uses: matter-labs/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZSYNC_DAPP_CHECKOUT_PROD }}"
          target: prod-mainnet
          projectId: zsync-dapp-checkout
          channelID: live

      - name: "Build ropsten [prod]"
        run: yarn run ci:build:ropsten
      - uses: matter-labs/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZSYNC_DAPP_CHECKOUT_PROD }}"
          target: prod-ropsten
          projectId: zsync-dapp-checkout
          channelID: live

      - name: "Build rinkeby [prod]"
        run: yarn run ci:build:rinkeby
      - uses: matter-labs/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: "${{ secrets.FIREBASE_SERVICE_ACCOUNT_ZSYNC_DAPP_CHECKOUT_PROD }}"
          target: prod-rinkeby
          projectId: zsync-dapp-checkout
          channelID: live